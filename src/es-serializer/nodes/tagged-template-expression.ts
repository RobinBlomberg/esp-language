import { TaggedTemplateExpression } from '../../es-ast';
import { Writer } from '../serialize';

export const writeTaggedTemplateExpression: Writer<TaggedTemplateExpression> = (
  node,
  write,
) => {};
